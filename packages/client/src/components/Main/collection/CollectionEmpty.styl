.empty
  display: flex
  align-items: center
  justify-content: center
  height 100%
  .wrap
    display: flex
    flex-direction: column
    align-items: center
    justify-content: space-around
    height 180px
    .photo
      height 80px
    .title
      font-weight bolder
      color var(--primary-color)