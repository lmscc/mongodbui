.search
  display flex
  flex-direction: column
  border 1px solid #e8edeb
  padding 10px
  margin-bottom 10px
  .condition
    margin-bottom 10px
  .moreCondition
    >div
      align-items: center
      .tag
        margin 0 8px
    .sort 
      flex: 1
    .input
      letter-spacing: 1px;
      ::placeholder//这个要放在input元素的父级元素上才生效
        color:gray
  button 
    margin 0 10px
